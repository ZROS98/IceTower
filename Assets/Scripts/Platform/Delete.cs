using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Delete : MonoBehaviour //RoofPenetration
{
    /*   [SerializeField] private Collider2D penetrationCollider;

       private void OnCollisionEnter2D(Collision2D other)
       {
       ///////
           foreach (ContactPoint2D contact in other.contacts)
           {
               if (contact.otherCollider.Equals(penetrationCollider))
               {
                   foreach (Collider2D playerCollider in gameObject.GetComponents<Collider2D>())
                   {
                       playerCollider.enabled = false;
                   }
               }
           }
       //////

           if (other.contacts[0].otherCollider.Equals(penetrationCollider))
           {
               foreach (Collider2D playerCollider in gameObject.GetComponents<Collider2D>())
               {
                   playerCollider.enabled = false;
               }
           }

           //чтобы перса не херачило наверх колайдером доски при падении делаем схему: 
   //колайдер персу возвращается не когда он только начал лететь вниз, а когда он коснулся верхней кромки доски
       }*/


}